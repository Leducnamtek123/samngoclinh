{
  "name": "samngoclinh",
  "version": "1.0.0",
  "private": true,
  "pnpm": {
    "overrides": {
      "shadboard-nextjs-full-kit>zod": "3.23.8",
      "@types/react": "19.0.12",
      "@types/react-dom": "19.0.4"
    },
    "packageExtensions": {
      "@hookform/resolvers": {
        "peerDependencies": {
          "zod": "*"
        }
      }
    }
  },
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "db:generate": "pnpm --filter @samngoclinh/api db:generate"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
